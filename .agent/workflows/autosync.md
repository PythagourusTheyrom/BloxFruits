---
description: Automatically sync local changes to GitHub
---
This workflow runs the `autosync.sh` script to add, commit, and push changes to the remote repository.

1.  Make sure you are in the project root.
2.  Run the synchronization script.

// turbo
3.  ./autosync.sh "AutoSync via Workflow"
